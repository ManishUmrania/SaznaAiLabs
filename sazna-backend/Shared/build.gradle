// shared/build.gradle

plugins {
    id 'java-library'
}

java {
    withSourcesJar()
}

jar {
    enabled = true
}

dependencies {
    // Keep minimal – only shared utilities
    implementation 'org.springframework.boot:spring-boot-starter-validation'
}